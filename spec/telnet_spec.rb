require '../src/hosts_check'
require '../net/telnet'
describe 'the Telnet Check application' do
	it 'checks that a telnet is possible i.e. libraries loaded and internet is available' do  
	end
	it 'handles a non-telnetable error correctly' do 
	end
	
end